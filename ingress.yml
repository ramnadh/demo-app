#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: $GKE_INGRESS
#spec:
#  defaultBackend:
#    service:
#      name: $GKE_SERVICE
#      port:
#        number: 80
        
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - http:
      paths:
      - backend:
          service:
            name: $GKE_SERVICE
            port: demoapp-http
        path: /
  #rules:
  #- http:
     # paths:
     # - path: /*
     #   pathType: ImplementationSpecific
     #   backend:
     #     service:
     #       name: $GKE_SERVICE
     #       port:
     #         number: 8080
     # - path: /demo-app
     #   pathType: ImplementationSpecific
     #   backend:
     #     service:
     #       name: $GKE_SERVICE
     #       port:
     #         number: 8080
